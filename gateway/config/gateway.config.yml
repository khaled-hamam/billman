http:
  port: 80

apiEndpoints:
  auth:
    paths:
      - '/auth'
      - '/auth/*'

serviceEndpoints:
  auth:
    url: 'http://auth'

policies:
  - jwt
  - log
  - proxy

pipelines:
  auth:
    apiEndpoints:
      - auth
    policies:
      - log:
          action:
            message: ${req.method} ${req.originalUrl}
      - proxy:
          action:
            serviceEndpoint: auth
            stripPath: true
